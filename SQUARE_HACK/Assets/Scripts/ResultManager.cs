using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ResultManager : MonoBehaviour {

	// Use this for initialization
	void Start () {
		//FadeSystem.instance.GetComponent<FadeSystem>().ChangeStagingType(1);
		//FadeUnit.instance.GetComponent<FadeSystem>().ChangeStagingType(1);
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
